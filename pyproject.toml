[tool.ruff]
line-length = 120
select = ["E", "F", "I", "N"]
ignore = ["E501"] # Ignore long lines for now to make CI green

[tool.ruff.isort]
known-first-party = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = "test_*.py"
